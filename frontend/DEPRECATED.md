# React UI — deprecated

This folder is archived. **Use Streamlit instead.**

From the project root:

```powershell
.\run_app.ps1
```

Open **http://localhost:8501**

The React stack was moved to `legacy_saas/` when possible. Do not run `npm run dev` for normal use.
